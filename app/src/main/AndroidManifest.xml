<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AmigoVirtual"
        tools:targetApi="31">

        <!-- Pantalla inicial: CargaActivity -->
        <activity
            android:name=".ActivityCarga"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pantalla de Login -->
        <activity android:name=".ActivityLogin" />

        <!-- Pantalla de Registro -->
        <activity android:name=".ActivityRegistro" />

        <activity android:name=".ActivityChat" />

        <activity android:name=".ActivityCreacionAmiko" />
    </application>

</manifest>
